'use client';

import { CommandPalette } from '@/components/command-palette';

/**
 * Client-side wrapper for CommandPalette to be used in Server Components
 */
export function CommandPaletteWrapper() {
  return <CommandPalette />;
}
