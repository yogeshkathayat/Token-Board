name: tokenboard

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-tokenboard}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME:-tokenboard}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${DB_USER:-tokenboard} -d ${DB_NAME:-tokenboard}']
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    image: ghcr.io/${ORG:-tokenboard}/tokenboard-api:${VERSION:-latest}
    # build: lets a self-hoster `make up` straight from a clone (compose builds
    # the image locally if it isn't already present), while CI can still build,
    # tag, and push this same name to a registry for pull-based deploys.
    build:
      context: ..
      dockerfile: apps/api/Dockerfile
      target: runtime
    restart: unless-stopped
    environment:
      NODE_ENV: production
      DATABASE_URL: postgres://${DB_USER:-tokenboard}:${DB_PASSWORD}@db:5432/${DB_NAME:-tokenboard}
      JWT_SECRET: ${JWT_SECRET}
      PUBLIC_URL: ${PUBLIC_URL}
      OIDC_ISSUER: ${OIDC_ISSUER:-}
      OIDC_CLIENT_ID: ${OIDC_CLIENT_ID:-}
      OIDC_CLIENT_SECRET: ${OIDC_CLIENT_SECRET:-}
      OIDC_REDIRECT_URI: ${OIDC_REDIRECT_URI:-}
      ALLOWED_EMAIL_DOMAINS: ${ALLOWED_EMAIL_DOMAINS:-}
      BOOTSTRAP_ADMIN_EMAIL: ${BOOTSTRAP_ADMIN_EMAIL:-}
      ALLOW_PASSWORD_SIGNUP: ${ALLOW_PASSWORD_SIGNUP:-true}
      SLOW_QUERY_MS: ${SLOW_QUERY_MS:-2000}
      USAGE_MAX_DAYS: ${USAGE_MAX_DAYS:-800}
      LEADERBOARD_REFRESH_DISABLED: ${LEADERBOARD_REFRESH_DISABLED:-false}
    healthcheck:
      test: ['CMD-SHELL', 'wget -qO- http://localhost:3000/api/v1/healthz || exit 1']
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
    depends_on:
      db:
        condition: service_healthy

  dashboard:
    image: ghcr.io/${ORG:-tokenboard}/tokenboard-dashboard:${VERSION:-latest}
    build:
      context: ..
      dockerfile: apps/dashboard/Dockerfile
      target: runtime
    restart: unless-stopped
    healthcheck:
      test: ['CMD-SHELL', 'wget -qO- http://localhost:80/ >/dev/null 2>&1 || exit 1']
      interval: 15s
      timeout: 5s
      retries: 5

  proxy:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf-available/nginx-tls.conf:ro
      - ./nginx/nginx-notls.conf:/etc/nginx/conf-available/nginx-notls.conf:ro
      - ./nginx/40-select-config.sh:/docker-entrypoint.d/40-select-config.sh:ro
      - ./nginx/ingest.njs:/etc/nginx/ingest.njs:ro
      - ./certs:/etc/nginx/certs:ro
    environment:
      INGEST_BEARER_HASH: ${INGEST_BEARER_HASH:-true}
      TLS_DISABLED: ${TLS_DISABLED:-false}
    depends_on: [api, dashboard]

volumes:
  pgdata:
